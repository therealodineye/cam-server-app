# This file can be empty.
# Its presence tells Python that the 'ffmpeg_manager' directory
# should be treated as a package, allowing us to import
# modules from each other (e.g., 'from views import app').